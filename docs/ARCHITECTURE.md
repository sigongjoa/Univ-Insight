# 🏗️ Univ-Insight 아키텍처

**버전:** 1.0
**최종 업데이트:** 2024-11-25
**상태:** Phase 1 완료

---

## 📊 전체 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                    Client Layer (UI/Frontend)                    │
│              (개발 예정: React + TypeScript)                     │
└────────────────────┬────────────────────────────────────────────┘
                     │ HTTP REST
┌────────────────────▼────────────────────────────────────────────┐
│                   API Layer (FastAPI)                            │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ GET /api/v1/universities                                 │   │
│  │ GET /api/v1/universities/{id}                            │   │
│  │ GET /api/v1/colleges/{id}                                │   │
│  │ GET /api/v1/departments/{id}                             │   │
│  │ GET /api/v1/professors/{id}                              │   │
│  │ GET /api/v1/research                                     │   │
│  │ GET /api/v1/research/{id}/analysis                       │   │
│  │ POST /api/v1/search (벡터 검색)                          │   │
│  └──────────────────────────────────────────────────────────┘   │
└────────────────────┬────────────────────────────────────────────┘
                     │
    ┌────────────────┼────────────────┐
    │                │                │
┌───▼────────────┐  │  ┌──────────────▼──┐
│  Services      │  │  │  Middleware    │
│  Layer         │  │  │  ┌──────────┐  │
├────────────────┤  │  │  │Exception │  │
│SNUCrawler      │  │  │  │Handler   │  │
│OllamaLLM       │  │  │  └──────────┘  │
│VectorStore     │  │  │                │
│Recommendation  │  │  │  ┌──────────┐  │
└────────────────┘  │  │  │Logging   │  │
                    │  │  └──────────┘  │
                    │  └────────────────┘
    ┌───────────────┴───────────────┐
    │                               │
┌───▼──────────────┐           ┌───▼──────────────┐
│  Data Layer      │           │  Vector Layer    │
├──────────────────┤           ├──────────────────┤
│SQLite            │           │ChromaDB          │
│(ORM Models)      │           │(Embeddings)      │
├──────────────────┤           ├──────────────────┤
│• universities    │           │• all-MiniLM-L6   │
│• colleges        │           │• cosine search   │
│• departments     │           │• 5 indexed docs  │
│• professors      │           └──────────────────┘
│• laboratories    │
│• research_papers │
│• paper_analysis  │
│• users           │
│• reports         │
└──────────────────┘
```

---

## 🔄 데이터 흐름 (Data Flow)

### 1. 초기 데이터 수집

```
SNUCrawler
    ↓ (웹 크롤링)
    ↓ 서울대학교 구조 데이터 추출
    ↓ 계층적 구조 파싱
    ↓
SQLite (ORM Models)
    ├── universities
    ├── colleges
    ├── departments
    ├── professors
    └── laboratories
```

**실행 방법:**
```bash
python run_real_pipeline.py
```

**결과:**
- 1개 대학
- 3개 단과대학
- 6개 전공
- 4명 교수
- 4개 연구실
- 5개 논문

---

### 2. LLM 분석

```
ResearchPaper (from DB)
    ↓ (제목 + 내용)
    ↓
OllamaLLM (llama2:latest)
    ├── 한국어 쉬운 설명 생성
    ├── 관련 직업 추출
    ├── 추천 회사 제시
    ├── 추천 학과목 제시
    └── 기술 스택 추출
    ↓
PaperAnalysis (저장)
    ├── easy_summary
    ├── job_roles
    ├── recommended_companies
    ├── recommended_subjects
    └── analysis_model
```

**실행 방법:**
```bash
python run_ollama_reanalysis.py
```

**결과:**
- 5개 논문 분석 완료
- 각 논문별 구조화된 분석 정보

---

### 3. 벡터 검색 준비

```
PaperAnalysis Data
    ↓ (제목 + 요약 + 메타데이터)
    ↓
VectorStore.add_embedding()
    ├── all-MiniLM-L6 모델로 임베딩
    ├── 메타데이터 저장
    └── ChromaDB에 저장
    ↓
ChromaDB
    ├── chroma_db/
    ├── 5개 문서 인덱싱 완료
    └── cosine similarity 검색 준비
```

**실행 방법:**
```bash
python run_chromadb_indexing.py
```

**결과:**
- 5개 논문 벡터화
- 의미 기반 검색 활성화

---

### 4. API 응답

```
클라이언트 요청
    ↓
GET /api/v1/universities
    ↓
FastAPI 라우터
    ↓
SQLAlchemy ORM 쿼리
    ↓
SQLite 데이터베이스
    ↓
JSON 응답 직렬화
    ↓
클라이언트 (JSON)
```

---

## 🗄️ 데이터 모델 (ERD)

```
┌──────────────────┐
│  universities    │
├──────────────────┤
│ id (PK)          │
│ name             │
│ name_ko          │
│ location         │
│ ranking          │
│ tier             │
└──────┬───────────┘
       │ 1:N
       ▼
┌──────────────────┐
│  colleges        │
├──────────────────┤
│ id (PK)          │
│ university_id(FK)│
│ name             │
│ name_ko          │
└──────┬───────────┘
       │ 1:N
       ▼
┌──────────────────┐
│  departments     │
├──────────────────┤
│ id (PK)          │
│ college_id (FK)  │
│ name             │
│ name_ko          │
└──────┬───────────┘
       │ 1:N
       ▼
┌──────────────────┐
│  professors      │
├──────────────────┤
│ id (PK)          │
│ dept_id (FK)     │
│ name             │
│ name_ko          │
│ title            │
└──────┬───────────┘
       │ 1:N
       ▼
┌──────────────────┐
│  laboratories    │
├──────────────────┤
│ id (PK)          │
│ professor_id(FK) │
│ name             │
│ name_ko          │
│ members          │
└──────┬───────────┘
       │ 1:N
       ▼
┌──────────────────┐
│ research_papers  │
├──────────────────┤
│ id (PK)          │
│ lab_id (FK)      │
│ title            │
│ abstract         │
│ full_text        │
│ keywords         │
└──────┬───────────┘
       │ 1:1
       ▼
┌──────────────────┐
│ paper_analysis   │
├──────────────────┤
│ id (PK)          │
│ paper_id (FK)    │
│ easy_summary     │
│ job_roles        │
│ companies        │
│ subjects         │
│ salary_range     │
└──────────────────┘
```

---

## 🔧 핵심 컴포넌트

### 1. SNUCrawler (크롤러)

**파일:** `src/services/snu_crawler.py`

**책임:**
- 서울대학교 웹 사이트 크롤링
- 계층적 데이터 구조 추출
- 에러 처리 및 재시도

**입력:** 없음 (자동 실행)
**출력:**
```python
{
    "university": {...},
    "colleges": [
        {
            "college": {...},
            "departments": [
                {
                    "department": {...},
                    "professors": [
                        {
                            "professor": {...},
                            "laboratories": [...]
                        }
                    ]
                }
            ]
        }
    ]
}
```

---

### 2. OllamaLLM (분석 엔진)

**파일:** `src/services/llm.py`

**책임:**
- 논문 콘텐츠 분석
- 구조화된 정보 추출
- JSON 파싱 (regex fallback)

**모델:** `llama2:latest`

**입력:**
```python
ResearchPaper(
    title="...",
    abstract="...",
    full_text="..."
)
```

**출력:**
```python
AnalysisResult(
    title="...",
    research_summary="...",
    career_path=CareerPath(...),
    action_item=ActionItem(...)
)
```

**성능:**
- 평균 분석 시간: 5-10초/논문
- JSON 파싱 성공율: 80%+

---

### 3. VectorStore (검색 엔진)

**파일:** `src/services/vector_store.py`

**책임:**
- 문서 임베딩
- 벡터 저장
- 유사도 검색

**임베딩 모델:** `all-MiniLM-L6-v2`
**저장소:** ChromaDB (persistent)
**유사도 메트릭:** cosine similarity

**API:**
```python
# 추가
add_embedding(paper_id, content, metadata)

# 검색
search(query, k=5, threshold=0.0)

# 업데이트
update_embedding(paper_id, content, metadata)

# 삭제
delete_embedding(paper_id)
```

**성능:**
- 검색 속도: <1초
- 인덱싱 속도: 0.5초/문서

---

### 4. FastAPI (REST API)

**파일:** `src/api/main.py`, `src/api/routes.py`

**책임:**
- HTTP 요청 처리
- JSON 응답 직렬화
- 에러 처리

**미들웨어:**
- Exception handler (글로벌)
- Logging middleware
- CORS middleware

**엔드포인트:**
```
GET  /api/v1/universities
GET  /api/v1/universities/{id}
GET  /api/v1/colleges/{id}
GET  /api/v1/departments/{id}
GET  /api/v1/professors/{id}
GET  /api/v1/research
GET  /api/v1/research/{id}/analysis
```

---

## 🧪 테스트 아키텍처

```
test_e2e_full_pipeline.py
├── test_database_layer()
│   ├── 대학/단과대학/전공 검증
│   ├── 교수/연구실 검증
│   └── 논문/분석 결과 검증
│
├── test_analysis_layer()
│   ├── LLM 분석 결과 검증
│   ├── 필드 완성도 검증
│   └── 모델 일관성 검증
│
├── test_vector_store_layer()
│   ├── 벡터 저장소 상태 검증
│   ├── 검색 기능 검증
│   └── 유사도 점수 검증
│
└── test_api_simulation()
    ├── 엔드포인트 응답 검증
    ├── 데이터 일관성 검증
    └── 계층적 네비게이션 검증
```

---

## 🔐 보안 고려사항

### 현재 구현
- ✅ 구조화된 로깅 (민감 정보 보호)
- ✅ 커스텀 예외 처리 (정보 유출 방지)
- ✅ 입력 검증 (Pydantic)

### Phase 2 계획
- [ ] JWT 인증
- [ ] Role-Based Access Control (RBAC)
- [ ] Rate limiting
- [ ] API key 관리

---

## 📈 확장성 (Scalability)

### Phase 1: 단일 서버
```
클라이언트 → FastAPI 단일 인스턴스 → SQLite → ChromaDB
```

### Phase 2: 로드 밸런싱
```
클라이언트들 → Nginx (로드 밸런서) → FastAPI 다중 인스턴스 → PostgreSQL → ChromaDB
```

### Phase 3: 마이크로서비스
```
클라이언트 → API Gateway →
  ├── University Service
  ├── Research Service
  ├── Analysis Service
  └── Search Service
```

---

## 🚀 배포 아키텍처

### 개발 환경 (현재)
```
로컬 머신
├── SQLite (./univ_insight.db)
├── ChromaDB (./chroma_db/)
└── FastAPI (localhost:8000)
```

### 프로덕션 환경 (계획)
```
클라우드 (AWS/GCP)
├── RDS PostgreSQL (데이터)
├── ElasticSearch (벡터 검색)
├── ECS/EKS FastAPI
├── CloudFront CDN
└── CloudWatch 모니터링
```

---

## 📝 API 응답 포맷

### 성공 응답
```json
{
  "status": "success",
  "data": {...},
  "timestamp": "2024-11-25T00:49:00Z"
}
```

### 에러 응답
```json
{
  "status": "error",
  "error_code": "NOT_FOUND",
  "message": "대학을 찾을 수 없습니다",
  "details": {...},
  "request_id": "uuid",
  "timestamp": "2024-11-25T00:49:00Z"
}
```

---

## 🔄 의존성 그래프

```
FastAPI
  ├─ SQLAlchemy (ORM)
  │  └─ SQLite
  ├─ Pydantic (validation)
  ├─ python-multipart
  └─ CORS

Services
  ├─ SNUCrawler
  │  └─ Crawl4AI
  ├─ OllamaLLM
  │  └─ ollama (Python client)
  ├─ VectorStore
  │  └─ ChromaDB
  └─ RecommendationEngine
     └─ numpy

Core
  ├─ Logging (JSON)
  ├─ Exceptions
  ├─ Middleware
  └─ Retry decorator

Testing
  ├─ pytest
  ├─ pytest-asyncio
  └─ requests
```

---

## 🎯 아키텍처 설계 원칙

1. **계층적 구조**: 프레젠테이션 → API → 서비스 → 데이터
2. **의존성 역전**: 상위 계층이 하위 계층에 의존
3. **관심사의 분리**: 각 모듈은 단일 책임
4. **DRY (Don't Repeat Yourself)**: 재사용 가능한 컴포넌트
5. **SOLID 원칙**: 유연하고 확장 가능한 설계

---

## 📚 참고 자료

- `PHASE_1_CORE_INFRASTRUCTURE.md` - Phase 1 상세
- `docs/api/API_ENDPOINTS.md` - API 명세
- `src/domain/models.py` - 데이터 모델
- `CLAUDE.md` - 프로젝트 개요

---

**마지막 업데이트:** 2024-11-25
**아키텍처 버전:** 1.0
**상태:** Phase 1 완료, Phase 2 준비 중
